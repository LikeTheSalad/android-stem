buildscript {
    ext {
        kotlin_version = '1.5.10'
        pluginMetadataTool_version = '1.0.0-SNAPSHOT6'
    }
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
        google()
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "com.likethesalad.tools:plugin-metadata-producer:$pluginMetadataTool_version"
        classpath "com.github.gmazzo:gradle-buildconfig-plugin:3.0.3"
        classpath 'com.likethesalad.tools:artifact-publisher:1.1.1'
    }
}

apply plugin: "artifact-publisher"

artifactPublisher {
    displayName = "Android String XML Reference"
    url = "https://github.com/LikeTheSalad/android-string-reference"
    vcsUrl = "https://github.com/LikeTheSalad/android-string-reference.git"
    issueTrackerUrl = "https://github.com/LikeTheSalad/android-string-reference/issues"
}

description = "This is a Gradle plugin for Android applications which resolves XML string references in other XML strings."
group = 'com.likethesalad.android'
version = '2.0.0-SNAPSHOT3'

ext {
    dagger_version = '2.39.1'
    androidBuild_version = '7.1.0'
    testingUtilities_version = '1.0.7-SNAPSHOT8'
    resourceLocator_version = '1.1.0-SNAPSHOT16'
    androidTools_version = "1.1.0-SNAPSHOT20"
}

allprojects {
    repositories {
        mavenLocal()
        mavenCentral()
        google()
    }
}