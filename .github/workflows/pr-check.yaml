name: PR Check

on:
  - pull_request
  - workflow_dispatch

jobs:
  checks:
    uses: LikeTheSalad/github-tools/.github/workflows/pr-check.yml@main
    with:
      java-version: '17'
      checks-command: ./check.sh
      run-on-windows: true
    secrets: inherit
